# cursor-starter

**AI Layer** — **живая самообучающаяся система** для Cursor. История версий — [`CHANGELOG.md`](CHANGELOG.md), принципы — [`.cursor/PRINCIPLES.md`](.cursor/PRINCIPLES.md).

При помещении в проект `ai-builder.md` анализирует его, генерирует хуки/команды/правила с нуля, обеспечивает дисциплину, а через `/reflect` — рефлексирует на основе git/валидатора и улучшает себя.

Не статичный шаблон. Не набор готовых примеров. Живой организм, который адаптируется, совершенствуется, не деградирует.

Полное изложение принципов и контура самоулучшения — в [`.cursor/PRINCIPLES.md`](.cursor/PRINCIPLES.md). История версий — в [`CHANGELOG.md`](CHANGELOG.md).

---

## 60-second Quick Start

| # | Шаг | Что сделать |
|---|---|---|
| 1 | **Открой репо в Cursor** | Скопируй содержимое `cursor-starter` в корень своего проекта (или используй как template). |
| 2 | **Стартуй чат с `@CURSOR.md`** | Это единственная точка входа: маршрутизация, правила, команды. |
| 3 | **Запусти bootstrap** | Слэш-команда `/bootstrap` (или вручную `@ai-layer-builder/ai-builder.md`) — пройдёт 8 шагов, сгенерирует хуки/правила под твой проект. |

После 5–10 задач — `/reflect`. Раз в неделю — `/audit`. Перед новым чатом — `/handoff`.

---

## Для кого этот шаблон

| Аудитория | Проблема | Решение |
|-----------|----------|---------|
| **Команды, которые используют Cursor** | Агент «разъезжается», ест контекст, выдумывает скилы, делает огромные дифы без плана | `CURSOR.md` + правила + команды + hooks = дисциплина |
| **AI-native инженеры** | Хотят показать не только код, но и **методологию** работы с агентами | Этот репозиторий как воспроизводимый артефакт процесса |
| **Техлиды / архитекторы** | Нужно внедрить AI-инструменты без хаоса | `ai-layer-builder/ai-builder.md` — сценарий переноса в любой проект |

---

## Как пользоваться

1. Склонируйте репозиторий или создайте новый проект из шаблона и откройте папку в **Cursor**.
2. В чате начните с **`@CURSOR.md`** — там порядок работы и таблица «задача → действие».
3. Чтобы **подстроить слой под свой стек и репо**, откройте **`@ai-layer-builder/ai-builder.md`** и выполните шаги **0 → 8** по порядку (merge с уже существующим `.cursor/`, без полной перезаписи без запроса).
4. Ведите **`PROJECT_KNOWLEDGE.md`**: после изменений обновляйте разделы (архитектура, стек, долги), а не журнал «что сделал».
5. План текущей задачи — **`PLAN.md`** или `.cursor/plans/*.md`; после задачи план можно очистить или перенести в планы.

---

## Состав репозитория

| Путь | Назначение |
|------|------------|
| [`CURSOR.md`](CURSOR.md) | Главная карта: маршрутизация, ссылки на rules и commands |
| [`PROJECT_KNOWLEDGE.md`](PROJECT_KNOWLEDGE.md) | Живая документация состояния (обновлять вместе с кодом) |
| [`PLAN.md`](PLAN.md) | План активной задачи (с примером структуры) |
| [`.cursor/plans/`](.cursor/plans/) | Архив планов (генерируются под проект) |
| [`.cursor/commands/reflect.md`](.cursor/commands/reflect.md) | Рефлексия и самоулучшение AI-слоя (`/reflect`) |
| [`AGENTS.md`](AGENTS.md) | Указатель на `CURSOR.md` для инструментов, которые ждут этот файл |
| [`ai-layer-builder/ai-builder.md`](ai-layer-builder/ai-builder.md) | Bootstrap и обновление AI-слоя |
| [`ai-layer-builder/spec-CURSOR.md`](ai-layer-builder/spec-CURSOR.md) | Спека структуры `CURSOR.md` |
| [`ai-layer-builder/spec-commands.md`](ai-layer-builder/spec-commands.md) | Формат проектных slash-команд |
| [`.cursor/rules/`](.cursor/rules/) | Политики Cursor (`*.mdc`) |
| [`.cursor/commands/`](.cursor/commands/) | Тексты для `/feature`, `/fix-bug`, `/review`, `/refactor` |
| `.cursor/hooks/` | Скрипты хуков (генерируются при bootstrap, могут отсутствовать в чистом шаблоне) |
| [`.cursorignore`](.cursorignore) | Что не индексировать (контекст и кредиты) |

**Hooks:** в чистом `cursor-starter` могут отсутствовать до запуска bootstrap-сценария.

---

## Slash-команды этого шаблона

В палитре команд Cursor (проектные, не `/create-*`):

| Команда | Файл |
|---------|------|
| `/feature` | `.cursor/commands/feature.md` — фича от ТЗ до PR |
| `/fix-bug` | `.cursor/commands/fix-bug.md` — воспроизведение → фикс → тест → док |
| `/review` | `.cursor/commands/review.md` — ревью дифа |
| `/refactor` | `.cursor/commands/refactor.md` — безопасный рефакторинг с тестами |
| `/reflect` | `.cursor/commands/reflect.md` — рефлексия на основе git/валидатора, не воображения |
| `/bootstrap` | `.cursor/commands/bootstrap.md` — обёртка над `ai-builder.md` |
| `/audit` | `.cursor/commands/audit.md` — структурный аудит AI-слоя |
| `/handoff` | `.cursor/commands/handoff.md` — короткий пакет контекста для нового чата |
| `/docs` | `.cursor/commands/docs.md` — синхронизация `PROJECT_KNOWLEDGE.md` с фактом |

---

## Новый репозиторий

Скопируйте содержимое **cursor-starter** в корень нового репо (или используйте как template), затем шаги из раздела «Как пользоваться». После появления кода обновите **`stack-specific.mdc`** и **`PROJECT_KNOWLEDGE.md`** (через агента по `ai-builder` или вручную).

---

## Уже есть проект с кодом

Смержите `.cursor/`, `CURSOR.md`, `PROJECT_KNOWLEDGE.md`, `ai-layer-builder/` с существующим деревом; затем **`@ai-layer-builder/ai-builder.md`** (режим brownfield в шаге 1) — чтобы правила и команды совпали с реальным стеком.

---

## Когда снова открывать `ai-builder.md`

Смена стека, крупный рефакторинг, новые команды/skills, расхождение документации и `.cursor/`. После прогона обновите дату в шапке **`CURSOR.md`**.

---

## Встроенные команды Cursor

`/create-rule`, `/create-hook`, `/create-skill` — создают артефакты в `.cursor/`; этим дополняют шаблон. Проектные сценарии — только файлы в **`.cursor/commands/*.md`** по [`spec-commands.md`](ai-layer-builder/spec-commands.md).

---

## `.cursorignore`

По умолчанию в игноре тяжёлые каталоги и **lock-файлы**. Если агенту нужен разбор зависимостей по lock-файлу — уберите соответствующие строки из [`.cursorignore`](.cursorignore).

---

## Пример структуры плана

См. [`PLAN.md`](PLAN.md) — там есть реальный пример и шаблон для копирования.

---

## Лицензия

MIT — используй свободно, адаптируй под свои проекты.